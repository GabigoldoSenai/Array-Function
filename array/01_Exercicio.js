let num = [4, 6, 2, 5]
//num.sort() //coloca em ordem

/*for(let pos in num ){ //para cada pos em num, faça:
    console.log(num[pos])
}
*/

console.log(`Vetor: ${num}`)
console.log(`Tamanho do vetor: ${num.length}`)
console.log(`A posição de 5 no vetor é ${num.indexOf(5)}`)
